export enum TodoListRouter {
  Name = "TodoList",
  Path = "/todo-list",
  Title = "Lista de Tarefas",
}

export enum PomodoroRouter {
  Name = "Pomodoro",
  Path = "/pomodoro",
  Title = "Pomodoro",
}
